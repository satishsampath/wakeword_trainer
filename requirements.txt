# Core dependencies
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.6.0
onnx>=1.14.0
onnxruntime>=1.15.0
onnxscript>=0.1.0
numpy>=1.24.0
soundfile>=0.12.0
scipy>=1.10.0
pydub>=0.25.0

# TTS APIs (optional - only needed for audio generation)
openai>=1.0.0
elevenlabs>=1.0.0

# Note: pydub requires ffmpeg for MP3 conversion
# Install ffmpeg:
#   Windows: winget install ffmpeg
#   macOS: brew install ffmpeg
#   Linux: sudo apt install ffmpeg
